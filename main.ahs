Loop
{
    tabState := GetKeyState("Tab", P)
 
    if (tabState = 1)
    {
        CoordMode, Mouse, Relative
        MouseClickDrag, Left, 300, -100, 100, -100, 2
    }
}                                                       
return